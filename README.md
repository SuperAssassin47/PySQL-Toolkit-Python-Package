# PySQL-Toolkit-Python-Package
PySQL is a toolkit package which allows you to easily initialise a new SQL database and create a new table inside of it. Without having to write out the entire coding logic.
